1.intent : 메시지 객체. OS는 항상 메시지를 받을 준비가 되어있는데 

브로드캐스트 리시버 : 문자메시지, 공지사항 띄우기(전원이 켜졌습니다) 등 ->화면에 보여줌

어떤 동작을 하라고 정해져있는 것-플래그
값 전달
액티비티 사이에 


서비스 : 정상적으로 돌리려면 스레드를 실행해야함. 화면과 별개로

근데 안드로이드는 서비스 작동이 많으면 화면도 안먹음.

---------

2. intent

인텐트를 던지는쪽은 

명시적 : 호출할 클래스를 항상 직접 지정.

암묵적 : 정의된 플래그를 호출. 시스템에서 만든 것, 커스터마이징, 생성 가능

액션.다이얼->전화걸기.
액션 : 값에 따라서 웹브라우저를 띄워줌



인텐트를 받는 쪽은

이걸 받아서 실행
ex: 액션 뷰 - 화면을 띄워줌

3. 액티비티 라이프 사이클 

액티비티 런칭 과정..

동작할 때는 따로 호출되는 게 없음 그냥 실행


액티비티A 실행중인데- > B로 호출


B가 온 스타트

-> 화면크기에 따라 사이클이 달라짐

완전히 가린다 : A는 퍼즈, 스탑

B : 실행(런타임), 끄면 스탑, 디스트로이

A : A 다시 시작

=> A는 그냥 스탑 상태. 메모리에 있지만 B가 엄청 크면 A를 죽일수 있음.
 저장 안되서 따로 필요

앱 전환 할때, 홈버튼 누를때
///


B가 반투명, 화면일부, A가 어쨌든 조금이라도 보일 경우 : A는 퍼즈 
(B가 한 픽셀이더라도 ㅋㅋ)



메모리에서 날아가지 않음. B가 커도 시스템이 죽일 수 없음 

여기서는 B는 아예 로드가 안됨



액티비티끼리만 작용. 

하나 실행했는데 하나가 계속 있으면 그건 뷰로 띄운 것. => Youtube . 플로팅뷰
탑에 띄운것. 액티비티 상관없이 돌아감

 

단!! 멀티를 지원하는 누가는 다름!!!







변수에 어떤걸 호출하겠다는 지정 -> 


컨텐트 프로바이더 


집에가면 자동으로


2. 기본 architecture


=====================================
안드로이드 업데이트


롤리팝

ART 리눅스 실행파일로 컴파일


사용자 알림. 머테리얼 디자인 적용

명시적 인텐드 

☆컨텍스트 블라인드 클래스 사용시 반드시 명시적으로

웹뷰 향상 : 파샤드. 확대하면 그 부분만 보여줌. 원래는 다 올라가는것.
둘다 메모리에는 있지만 화면에 보이는 것
크롬..


마시멜로

런타임 퍼밋이 생김
안드로이드 
해당 모듈. 접근. 확인 창이 뜸 (예전엔 안떴음)

에너지 절약 : 다즈모드 . 한시간이 지나면 앱 자동으로 죽음.
사용자가 쓸지 안쓸지 결정

아파치. 
기존 앱에서 실행되던거 거의다 안될 정도로 바뀜..

지문인식 

앱 링킹
한번만 연결하면 계속 같은 걸로
=> 이 URL 연결을 위해 어떤 브라우저를 사용하시겠습니까?
매번 선택 했던게 한번하면 계속 같은 브라우저로 실행



누가

액티비티 라이프사이클

멀티 윈도우

드래그앤 드랍으로 이미지 이동. 동시에 두개창 실행



라이프사이클

가장 최근에 사용한것만 계속활성화 -> 나머지는 퍼즈

동영상일 경우 실제로 동시에 사용하는게 아니게됨.

=> 그렇게 하도록 코딩을 짜줘야...안드로이드에서 자동으로 해주는 건 아님 ㅠ

우선순위는 퍼즈

.... 현재 런 인게 끊길 수 있음;; 감안해야

* '밴더'사의 특수한 것들 (LG G2 같은경우 ) 는 동시 실행 작동 가능.



동영상 

액티비티 실행 . 라이프사이클에 영향을 받지않음


스택방식 

내가 최근에 실행한 게 가장 먼저 빼올 수 있음

OS가 스택에서는 죽이는데

리스토어 인스탄스스테이트로 값 저장해서

온 크리에이트에서 호출해서 다시쓸수 있음





안드로이드의 실제 설정파일 -매니페스토
하나하나 액티비티 설정해주는 곳


인텐트 줘도 여기 설정안되있으면 호출 안됨

필터 설정을 해주면 

메인은 아무것도 안해도 호출 : 매니페스토에 인텐트 설정되어있기 때문

      <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>



<style name = "Theme.Appcompat.Translucent">


<item name = "windowBackground"> @color/transparent</item>

<item name = "android:windowTranslucent">true</item>




//아이템이 각 속성에 적용. 바탕화면이 투명해징


==================================================================

Intent에 있는 메시지 하나하나를 Extra라고 함

Extra가 묶음으로 되어있는게 Bundle

int String




//액티비티로 값 넘기기
1. 인텐트 생성

2. putExtra 함수에 전달할 값 설정


switch 공통적인 것은 case 전후로 빼면 됨

3. Extra에 담긴 값





Common에 설정


1. intent
Intent intent = getIntent();

번들 클래스 안에 가져올수 있는 게 있음
내가 넘길 값의 타입에 맞는 것들


엑스트라에서 번들 묶을 꺼내기
Bundle bundel =inten.get Extras



번들에서 변수 타입에 맞게 직접 꺼내기

스트링 
스트링 스트링. 번들. 겟스트링(var);
tv.setText(string)



===========================================================


Uri
통합자원 식별자

인터넷 혹은 핸드폰에 있는 사진 등 데이터들은 모두  URI를 가진다.

인터넷 주소같은것


 Value는 EditBox의 값을 가져옴 

tel: 이라는 프로토콜을 가진
http://

=======================
 


URI인식 프로토콜이 있음.






----------------------------------


web-view

브로우저

패딩값이 전체화면
================================================================



switch
case R.id.
Intent intent = new intent



